<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>课程列表</title>
<%- include('script.ejs')%>
</head>
<body>
	<div class="container">

		<div id="header"><%- include('header.ejs')%></div>

		<!-- 内容 -->
		<div class="row" style="margin-top: 10px">
            <div id="left" class="col-lg-2"><%- include('left.ejs')%></div>

			<div class="col-lg-7 jumbotron"
				style="background-color: #e3f2fd; padding: 1rem; text-align: center;">
				<h4>课程列表</h4>
				<div class="card">
					<div class="card-body">
						<table class="table">
							<thead style="background-color: #e3f2fd;">
								<tr>
									<th scope="row">序号</th>
									<th scope="row">课程名称</th>
									<th scope="row">开设年级</th>
									<th scope="row">授课教师</th>
									<th scope="row">报名</th>
									<th scope="row">课程成员</th>

								</tr>
							</thead>

							<tbody >
                                <% for(var i=0;i<cources.length;i++){ %>
                                    <tr id="cou>">
										<th scope="row"><%=i+1%></th>
										<td><%=cources[i]._doc.name%></td>
										<td><%=cources[i]._doc.grade%></td>
										<td><%=cources[i]._doc.teacher%></td>
                                       
										<% if(!flag[i]){ %>
						
											<td><a class="btn btn-outline-danger"
												href="/regcource?flag=0&cid=<%= cources[i]._doc._id %>">报名</a></td>
										
                                        <% } %>									
										<% if(flag[i]){ %>
											<td><a class="btn btn-outline-primary"
												href="/regcource">取消</a></td>
                                         <% } %>	

										
										<td><a class="btn btn-outline-success"
											href="CourceInfo?kc_id=${cource.kc_id}">详情</a></td>
									</tr>
                               <% } %>
															
							</tbody>
						</table>
						<nav aria-label="Page navigation example">
							<ul class="pagination">
								<li class="page-item"><a class="page-link" href="#"
									id="btnp">Previous</a></li>
								<c:forEach begin="1" end="${Math.ceil(cources.size()/5)}" var="i">
									<li class="page-item"><a class="page-link" href="#"
										id="btn${i}">${i}</a></li>
								</c:forEach>
								<li class="page-item"><a class="page-link" href="#"
									id="btnn">Next</a></li>
							</ul>
						</nav>
					</div>
				</div>

			</div>

            <div id="right" class="col-lg-3"><%- include('right.ejs')%></div>
		</div>

	</div>
</body>
<script>

</script>
</html>